#!/usr/bin/make -f
DPKG_EXPORT_BUILDFLAGS = 1
include /usr/share/dpkg/default.mk

export NO_PKG_MANGLE=1
export QA_RPATHS=$[ 0x0002 ]
export NJBS = `nproc`

DESTDIR = $(CURDIR)/debian/tmp

%:
	dh $@ --parallel --with autotools-dev

override_dh_auto_configure:
	./configure \
	  --prefix="/usr/lib/openresty-debug" \
	  --with-debug \
	  --with-cc-opt="-I/usr/lib/openresty/zlib/include -I/usr/lib/openresty/pcre/include -I/usr/lib/openresty-debug/openssl/include -O0" \
	  --with-ld-opt="-L/usr/lib/openresty/zlib/lib -L/usr/lib/openresty/pcre/lib -L/usr/lib/openresty-debug/openssl/lib -Wl,-rpath,/usr/lib/openresty/zlib/lib:/usr/lib/openresty/pcre/lib:/usr/lib/openresty-debug/openssl/lib" \
	  --conf-path=/etc/openresty-debug/nginx.conf \
	  --http-log-path=/var/log/openresty-debug/access.log \
	  --error-log-path=/var/log/openresty-debug/error.log \
	  --lock-path=/run/lock/openresty-debug.lock \
	  --pid-path=/run/openresty-debug.pid \
	  --modules-path=/usr/lib/openresty-debug/modules \
	  --http-client-body-temp-path=/var/lib/openresty-debug/body \
	  --http-fastcgi-temp-path=/var/lib/openresty-debug/fastcgi \
	  --http-proxy-temp-path=/var/lib/openresty-debug/proxy \
	  --http-scgi-temp-path=/var/lib/openresty-debug/scgi \
	  --http-uwsgi-temp-path=/var/lib/openresty-debug/uwsgi \
	  --with-pcre-jit \
	  --without-http_rds_json_module \
	  --without-http_rds_csv_module \
	  --without-lua_rds_parser \
	  --with-stream \
	  --with-stream_ssl_module \
	  --with-http_v2_module \
	  --without-mail_pop3_module \
	  --without-mail_imap_module \
	  --without-mail_smtp_module \
	  --with-http_stub_status_module \
	  --with-http_realip_module \
	  --with-http_addition_module \
	  --with-http_auth_request_module \
	  --with-http_secure_link_module \
	  --with-http_random_index_module \
	  --with-http_gzip_static_module \
	  --with-http_sub_module \
	  --with-http_dav_module \
	  --with-http_flv_module \
	  --with-http_mp4_module \
	  --with-http_gunzip_module \
	  --with-threads \
	  --with-file-aio \
	  --with-poll_module \
	  --with-luajit-xcflags='-DLUAJIT_NUMMODE=2 -DLUAJIT_ENABLE_LUA52COMPAT -O0' \
	  --with-dtrace-probes \
	  -j$(NJBS)

override_dh_usrlocal:

override_dh_auto_install:
	dh_auto_install
	rm -rf $(DESTDIR)/usr/lib/openresty-debug/luajit/share/man
	rm -rf $(DESTDIR)/usr/lib/openresty-debug/luajit/lib/libluajit-5.1.a
	mkdir -p $(DESTDIR)/usr/bin
	cd $(DESTDIR)/usr/bin
	ln -sf ../lib/openresty-debug/nginx/sbin/nginx $(DESTDIR)/usr/bin/openresty-debug
	cd $(CURDIR)

override_dh_strip:
	dh_strip --dbg-package=openresty-debug-dbgsym
